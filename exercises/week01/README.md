# Week 1 Exercises

## Meeting 1
- Open `meeting1_token_explorer.ipynb`.
- Run cells top-to-bottom.
- Complete and test `count_tokens(prompt, model)`.
- Diagnose and fix the two deliberate bugs in the API call cell.

### Requirements
```bash
pip install tiktoken openai
```

If OpenAI is unavailable, use a fallback provider (for example via `litellm`) as discussed in class.
